'use strict';

/**
 * category-connectivity controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::category-connectivity.category-connectivity');
