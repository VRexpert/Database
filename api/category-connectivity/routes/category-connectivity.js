'use strict';

/**
 * category-connectivity router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::category-connectivity.category-connectivity');
